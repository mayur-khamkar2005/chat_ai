# Context Directory

This folder is intended for React Context API setups. 
If your React app needs global state (like a Light/Dark theme toggle or user authentication status) that many components need to access without "prop drilling", the context providers go here.
