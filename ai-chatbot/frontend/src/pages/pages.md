# Pages Directory

This folder is meant for top-level Page components (like `Home.jsx`, `About.jsx`, or `Chat.jsx`). 
While smaller pieces go in the `components` folder, those pieces are assembled together into full pages here. This is especially useful when using a library like `react-router-dom`.
