# Components Directory

This folder holds all of the reusable React building blocks for the UI.

In modern React applications, it is best practice to break the UI down into small, manageable, and isolated pieces. For example, the `ChatScreen` component lives here. It handles rendering the visual layout of the chat interface, which is then imported and used by the main `App.jsx` file.
