# Utils Directory (Frontend)

This folder contains helper functions that don't rely on React. 
For example, a function that capitalizes the first letter of a string or calculates the time elapsed since a message was sent would live here.
