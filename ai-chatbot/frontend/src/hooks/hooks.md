# Hooks Directory

This folder is for custom React Hooks (e.g., `useWindowSize`, `useFetchChat`). 
By extracting complex `useEffect` or `useState` logic into custom hooks here, you keep your main UI components clean and readable.
