# Controllers Module

## Purpose
The controllers module interfaces directly with incoming HTTP requests. It receives request data and passes it to the brain or service layer for processing.

## Responsibilities
- handle API requests
- call the chatbot workflow
- return responses to the client

## Future direction
This module can later be expanded with validation, request parsing, and richer API behavior.
