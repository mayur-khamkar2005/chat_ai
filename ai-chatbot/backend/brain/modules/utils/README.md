# Utils Module

## Purpose
The utils module contains small reusable helper functions that support multiple parts of the backend.

## Responsibilities
- centralize common helper logic
- reduce duplication across modules
- keep the codebase easier to maintain

## Future direction
This module can grow as more shared helpers become necessary.
