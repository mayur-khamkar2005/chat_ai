# Services Directory

The services folder is meant for external API integrations or complex business logic. 
If your chatbot needs to talk to OpenAI, a weather API, or send an email, the code to perform those third-party actions should be written as a "service" here and then called by the controller.
