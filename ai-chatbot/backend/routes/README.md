# Routes Module

## Purpose
The routes module defines the API entry points for the backend. It connects HTTP endpoints to the appropriate controller logic.

## Responsibilities
- expose REST-style endpoints
- route requests to controllers
- keep the API surface organized

## Future direction
This module can later be expanded with versioned routes, more endpoints, and richer API conventions.
