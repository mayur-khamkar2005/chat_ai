# Routes Directory

This folder is responsible for defining the API endpoints (URLs) that the frontend can call.

## Key Files
- **`chat.route.js`**: This file maps the incoming HTTP POST requests for `/api/chat` directly to the `chatController` function (located in the `/controllers` folder). Routes keep the API organized by separating URL definitions from the actual logic.
