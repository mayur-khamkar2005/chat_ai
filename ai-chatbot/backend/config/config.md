# Config Directory

This folder is intended to hold configuration files for the backend. 
While it may not be actively used right now, in the future, it is typically used for things like database connection setups (e.g., MongoDB, PostgreSQL), third-party API key configurations, or environment-specific variable mapping.
