# Constants Module

## Purpose
The constants module stores reusable values that should not be hardcoded in multiple places. This keeps the backend consistent and easier to update.

## Responsibilities
- centralize shared string values
- simplify configuration references
- reduce duplication across modules

## Future direction
This module can be expanded with more shared constants as the architecture grows.
