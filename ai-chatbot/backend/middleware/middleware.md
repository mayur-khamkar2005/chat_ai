# Middleware Directory

This folder is used for Express middleware functions. 
Middleware are functions that run *before* the request reaches the controller. Common uses for this folder include:
- Authentication guards (checking if a user is logged in).
- Request logging.
- Error handling scripts.
